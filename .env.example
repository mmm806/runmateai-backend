# 데이터베이스
DB_URL=jdbc:postgresql://localhost:5433/runmateai
DB_USERNAME=postgres
DB_PASSWORD=postgres456

# JWT (기존 application.yml에 있던 값 그대로 입력)
JWT_SECRET=

# Claude API (기존 application.yml에 있던 값 그대로 입력)
CLAUDE_API_KEY=

# 개발 환경 설정
DDL_AUTO=update
SHOW_SQL=true
LOG_LEVEL=DEBUG
HIBERNATE_LOG_LEVEL=DEBUG
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174